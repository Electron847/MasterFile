server {
#  include "common"
  akka {
#    loglevel = "DEBUG"
    actor {
      provider = remote
    }
    remote {
      enabled-transports = ["akka.remote.netty.tcp"]
      netty.tcp {
        hostname = "140.192.37.109"
        port = 2552
      }
#      log-sent-messages = on
#      log-received-messages = on
    }
  }
}
